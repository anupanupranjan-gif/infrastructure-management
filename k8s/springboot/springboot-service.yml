apiVersion: v1
kind: Service
metadata:
  name: advanced-search-service
  namespace: commerce
  labels:
    app: advanced-search-app
spec:
  selector:
    app: advanced-search-app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP